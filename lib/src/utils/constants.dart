import 'package:flutter/material.dart';

class AppConstants {
  // API Endpoints
  static const String apiUrl = 'https://jsonplaceholder.typicode.com';

  // App-specific Constants
  static const int maxPasswordLength = 16;

  // Colors
  static const primaryColor = const Color(0xFF375495);
  static const secondaryColor = const Color(0xFF52B3EC);
}
